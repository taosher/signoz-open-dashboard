# bug-track.md

> Pitfall log. Whenever development/debugging/testing hits a pitfall, append a row immediately. Never log a plaintext API key (only write `effectiveKeySource/effectiveKeyHash(first 8 chars)`).

| Date | Phenomenon | Root cause | Handling | Status | Related |
|---|---|---|---|---|---|
| 2026-09-18 | `pnpm test:e2e` reports `test/jest-e2e.json is not valid JSON` | The file is actually JS (`module.exports`) with a mistaken `.json` extension | Renamed to `test/jest-e2e.js` and synced the `test:e2e` script in `package.json` | Resolved | M1 e2e |
| 2026-09-18 | `query_range POST` e2e case hangs on timeout (5s), the other 6 pass | The Express/Nest default JSON bodyParser has already consumed the request stream, so the controller listening on `req on('data'/'end')` never sees the events | Forward using the already-parsed `req.body` re-serialized, and note in the controller comment that manually reading the stream is forbidden | Resolved | M1 proxy forwarding |
| 2026-09-18 | e2e `Config validation error: SIGNOZ_BASE_URL is required` | `ConfigModule.forRoot` validates at import-decorator evaluation time, while the env is only set in `beforeAll` | Added `test/setup-e2e.ts`, injected via jest `setupFiles` to set a placeholder env before imports | Resolved | M1 e2e |
| 2026-09-18 | `pnpm test` fails in the shared package with `jest: command not found` | shared declares `jest --passWithNoTests` but never installed jest | Changed shared `test` to `tsc --noEmit` (the package has no unit tests yet; typechecking is the verification) | Resolved | M1 test |
| 2026-09-18 | Restarting the service reports `EADDRINUSE :::8080`, curl hits the old process | The old `node dist/main.js` was not fully killed (pid file overwritten), so acceptance traffic went to the wrong process | Kill with `pkill -f apps/api/dist/main.js` + confirm the port is free via `lsof -i :8080` before restarting | Resolved | M1 integration |
| 2026-09-18 | web build fails with `EMBED_ERROR_CODES is not exported by shared/dist` | shared dist is CJS, which Vite/Rollup cannot resolve through the `export *` re-export chain | web imports the shared TS source directly via vite alias + tsconfig paths (dist is still used by NestJS) | Resolved | M2 build |
| 2026-09-18 | Embed page blank (`#embed-root` empty) | vite `base './'` makes the bundle resolve relative paths under `/embed/:id` to 404 | Changed base to `/` (the service is always mounted at the root path) | Resolved | M2 integration |
| 2026-09-18 | With `locale=en`, time/refresh presets still show Chinese | Preset labels were hardcoded in Chinese | Added `timePresetOptions(locale)` / `refreshPresetOptions(locale)` | Resolved | M3 i18n |
| 2026-09-18 | Browser CDP screenshots timed out twice, no acceptance screenshots | `Page.captureScreenshot` times out in this environment (the page contains many canvases) | Switched to DOM-assertion acceptance (canvas count/copy/empty states); screenshots recorded as an environment limitation | Known limitation | M4 acceptance |
| 2026-09-18 | Embed page looks far from native (echarts default styling) | Native time-series charts use uPlot + the theme palette + the grafana unit system; we used default styling | Visual-alignment track: switch to the same uPlot engine, copy palette/grid/tokens/formatting verbatim (`signoz/{theme,colors,format}.ts`, `UplotChart.tsx`) | Deprecated (M4 rollback to self-built UI) | Visual alignment |
| 2026-09-19 | Embed page blank with React #311 (`Should have a queue`, inside useTranslation) | react-i18next returned early on one path (1 hook) before the i18n singleton was ready, then took the full path (5 hooks) once ready; the dynamic `import('ReactI18')` made the first render land on the not-ready branch, so the re-render after fetch resolved had a mismatched hook count | Switched to static imports (same as the upstream index.tsx) so the singleton is ready before render | Resolved (code removed with the vendor rollback, conclusion archived) | 100% replica |
| 2026-09-19 | GridGraphs appeared frozen at mount, main thread unresponsive | False alarm: a poisoned ego-browser tab (an old frozen page) polluted all subsequent observations + stale bundle/tab-title misreads; a real-device retest rendered normally | Verify in an isolated single space and treat server logs as ground truth; AGENTS now has the single-TaskSpace hard rule | Resolved (methodology) | 100% replica |
| 2026-09-19 | M4 decision: fully roll back to self-built UI + theme plugin architecture, `src/vendor/` (1300+ files) deleted | The 100% replica's dependency closure/Provider chain/upgrade burden was too high and conflicted with the multi-theme extension goal | Deleted `src/vendor/` + take-vendor/apply-patches/stubs; kept the reference snapshot (reproducible, not committed); rewrote design doc §7 | Done | M4 decision |
| 2026-09-19 | M5 self-built widget query 400 `unknown field "dataType" in query spec` (first panel errored, the rest fine) | `signoz/queryPayload` passed the widget's raw frontend-shaped `groupBy` (`{key,dataType,type}`) straight through, but the backend only accepts the API shape (`{name,fieldDataType,fieldContext}`) | Added `mapGroupBy`/`mapSelectFields` (against the snapshot `createBaseSpec`); `selectColumns→selectFields` fixed the same way | Resolved | M5 integration |
| 2026-09-19 | After `pkill -f apps/api/dist/main.js` the port was still held, the new instance hit EADDRINUSE and curl reached the old process (uptime gave it away) | The old process had started with an absolute path without suffix (`.../dist/main`), so the `main.js` match string missed it | Check the command line via `ps` before killing (by PID if needed), and confirm via `curl healthz` uptime that the new process is serving | Resolved | Local integration |
| 2026-09-19 | Embed tooltips/legends show raw `{{mountpoint}}` text and the internal name `__result_0` | (1) The formula F1 legend template (`{{mountpoint}}`) was never substituted from labels; (2) response aggregation carries the backend-internal `alias='__result_0'`, and our alias-first priority leaked it | Legends now follow the observable `getLabelName`/`getLegend` behavior: template substitution → single-label bare value → legendMap → queryName; time-series naming ignores the response alias | Resolved | M5 polish |
| 2026-09-19 | Pie slices show `A#0/B#0` with no center total; time-series tooltips show raw values | scalar returns series-shaped data (aggregations+series) for pie instead of columnar data, but we only parsed columns+data; tooltips used the echarts default formatter | Dual-shape scalar parsing (columnar→tables, series-shaped→series); pie slices by column name/series name + center total + formatted tooltip; time-series tooltips use a custom formatter (time + `formatValue`) | Resolved | M5 polish |
| 2026-09-19 | New dashboards show variable IDs in the dropdown and fetch nothing (D1/D2 tables empty) | Dashboard JSON keys variables by id, but we used the record key instead of applying `v.name`; the two variable APIs also wrap results in `data`, which we read from the top level | Added `normalizeVariables` to normalize by name; `variables/query` now reads `data.variableValues`, `fields/values` reads `data.normalizedValues` (also merging a bare `values` shape) | Resolved | Multi-dashboard triage |
| 2026-09-19 | D1 pod-status (clickhouse_sql) always empty because its payload was skipped | Every widget always carries placeholder arrays `promql/ch: [{query:''}]`; we dispatched on "array existence", so the empty promql placeholder swallowed CH | Dispatch only on `queryType` (builder/promql/clickhouse_sql); empty placeholder queries are still filtered as before | Resolved | Multi-dashboard triage |
| 2026-09-19 | D1 dependent-variable query 400 (`... = test` illegal SQL) | When dependent variables are substituted into the query text, strings were not quoted (the `$var` in filters is handled by the backend; only the SQL-text pre-substitution needs care) | `formatValueForQuery` wraps strings in single quotes and expands arrays to `'a','b'` | Resolved | Multi-dashboard triage |
| 2026-09-19 | Charts overflow their cards (x-axis labels / bottom legends clipped) | Fixed 300px height + insufficient grid bottom reserve (40px cannot fit axis labels + two legend rows) | Card body switched to a flex column with the chart flex-sizing itself + ResizeObserver; grid `bottom:64 + containLabel`, with bottom whitespace reserved for the legend | Resolved | Visual polish |
| 2026-09-19 | Dark mode cards/charts all white (`?mode=dark` cold start looks broken) | `ColorModeProvider` was nested inside `TokensProvider`, so the antd ConfigProvider always read the default light | Moved `TokensProvider` inside `EmbedApp`, wrapped by `ColorModeProvider`; main keeps only the legacy token for the fatal path | Resolved | Visual polish |
| 2026-09-19 | Pie center total sits too high, pressing against the ring top | The overlay used `padding-bottom: 12%` for centering, but percentage padding resolves against **width** (71px off on a 596px card), pushing the center to 36% | Switched to absolute positioning with `height: 88%` (percentage height resolves against parent height), exactly aligned with the pie center at `center 44%` | Resolved | Visual polish |
| 2026-09-19 | antd icons 4.8 type errors under `@types/react@18.3` (`onPointerEnterCapture` missing) | antd icons v4 Icon types are incompatible with the new JSX types | legacy/shadcn both switched to lucide icons, `@ant-design/icons` dependency removed | Resolved | shadcn theme |
| 2026-09-19 | shadcn line/bar charts have no legend | `LineChart/BarChart` only mounted a Tooltip and omitted `<Legend content>` | Added `ChartLegendContent` (re-verified via screenshot) | Resolved | shadcn theme |
| 2026-09-19 | Under shadcn, 40+ series legends/tooltips blow up the layout | Legends wrapped over every item and tooltips listed everything (thread-pool panel) | Legends/tooltips now converge to the first 12 items + `+N` (`MAX_LEGEND_ITEMS`), keeping tooltips readable | Resolved | shadcn polish |
| 2026-09-19 | Base UI Select shows the raw value (`30m`) instead of the label | With the portal closed the items unmount, so `Select.Value` cannot find the selected item text | Added an explicit caller-provided `displayValue` for the display copy | Resolved | shadcn theme |
| 2026-09-19 | shadcn layout fully collapsed: panels stack full-width vertically | flex `width: calc(50% - 6px)` only accounts for half of a 12px gap; under the shadcn 24px gap two items + gap overflow, wrap, and get stretched full-width by flex-grow | Switched to a 12-column CSS grid (`span w`), gaps handled natively so gap changes no longer wrap | Resolved | shadcn regression |
| 2026-09-19 | Base UI dropdowns land at the top-left / cover their trigger, nearly unusable | Base UI v1 RC Positioner positioning is unstable (listbox with `position: static` lands top-left) | Rolled back to radix (previously screenshot-verified); charts use Recharts directly anyway and are unrelated to radix/Base, keeping the shadcn style classes | Resolved | shadcn regression |
| 2026-09-19 | Clicking a chart leaves a blue focus ring that will not go away | Focus actually lands on a `<g>` inside the svg, which the previous wrapper-level CSS did not cover | Added `.schn svg :focus { outline: none }`, verified `outline: none` on a real-device click | Resolved | shadcn regression |
| 2026-09-19 | shadcn layout collapsed again: panels stack full-width vertically (under 24px gap) | flex `width: calc(% - 6px)` was hardcoded for a 12px gap; under a 24px gap two items squeeze, wrap, and get stretched full-width | Switched to a 12-column CSS grid (`span w` + sort by (y,x) replicating the console), gaps handled natively | Resolved | shadcn regression |
| 2026-09-19 | shadcn buttons unstyled (2px outset native border) | Without preflight the native button styling remains, and toggle/ghost items missed `border-0` | Added `border-0 bg-transparent` to toggle items (ghost likewise), plus `font: inherit` on `.schn button` | Resolved | shadcn regression |
| 2026-09-19 | recharts v3 Pie ignores `center` (center text sits too high) | v3 only honors `cx/cy`; `center` is silently ignored and the pie center stays at 50% | Changed to `cx="50%" cy="44%"`, aligned from the same source as the HTML overlay | Resolved | shadcn regression |
| 2026-09-19 | shadcn buttons unstyled (2px outset native border, text larger than the label) | UA styles remain without preflight; additionally the `font: inherit` shorthand (layerless) overrode the tailwind `text-xs` | Only add `font-family: inherit`; toggle/ghost items get `border-0 bg-transparent` | Resolved | shadcn regression |
| 2026-09-19 | day-picker selected date is a black block swallowing the text | v10 paints selected on the cell while the button keeps dark text (the classes live on two different elements) | `.schn [data-selected] > button` inverted + background removed (one rule each for light/dark) | Resolved | shadcn polish |
| 2026-09-19 | Calendar month arrows pile up on the right / float to the panel middle | v10 places nav as a sibling of months (not a caption child), so `right-0` stacks and `top-1/2` centers against the whole panel | nav changed to `absolute inset-x-0 top-0 h-9 justify-between`, same height as the title row, spread to both ends | Resolved | shadcn polish |
| 2026-09-19 | Calendar month arrows unstyled (outset border) | radix/rdp nav buttons miss `border-0` (same preflight pitfall) | Added `border-0 bg-transparent` | Resolved | shadcn polish |
| 2026-09-19 | Custom floating layer drifts to the top-left, gray backdrop only covers the toolbar | The layer sits inside the toolbar with `backdrop-blur`, so `fixed inset-0` uses the toolbar as its containing block | Switched to the SigNoz-style single popover: quick-pick grid + custom range in the same panel, anchored under the time button, no modal and no fullscreen gray backdrop | Resolved | shadcn polish |
